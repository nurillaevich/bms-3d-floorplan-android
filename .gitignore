.gradle/
build/
app/build/
local.properties
# Web assets are copied in by CI (standalone kiosk page + built card bundle).
app/src/main/assets/kiosk/
*.iml
.idea/
# Signing keystore is injected from a GitHub Actions secret at build time —
# NEVER commit it (public repo). CI base64-decodes secrets.SIGNING_P12_BASE64
# into app/signing.p12 before assembling.
app/signing.p12
*.p12
*.jks
*.keystore
signing.p12.base64.txt
